apiVersion: v1
kind: Service
metadata:
  name: devops-demo-api
  namespace: devops-demo
  labels:
    app: devops-demo-api
    project: devops-pipeline-demo
    environment: dev
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: devops-demo-api
